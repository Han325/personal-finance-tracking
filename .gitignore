# Runtime data — never commit real bank statements or processed output
test-files/
test-files-*/
sample-files/
/*.pdf
/*.csv

# Build output (webapp/ has its own too, but this catches anything at root)
dist/
build/

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/
